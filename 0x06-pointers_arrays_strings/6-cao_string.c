#include "main.h"

/**
 * cap_string - capitalizees all words of a string
 * @str: parameter to be capitalized
 *
 * Return: pointer to a string
 */
char *cap_string(char *str)
{
	int i = 0;

	while (str[i])
	{
		while (!(str[i] >= 'a' && str[i] <= 'z'))
		{
			i++;
		}
		if (
	}
	
}
